services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: signin-ui
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  signin-ui:
    build: .
    ports:
      - "8080:8080"
      - "8081:8081"
    environment:
      DATABASE_HOST: db
      DATABASE_PORT: 5432
      DATABASE_NAME: signin-ui
      DATABASE_USER: postgres
      DATABASE_PASSWORD: postgres
    depends_on:
      - db
    env_file:
      - .env

volumes:
  postgres_data:
